{
  "name": "scrolly",
  "version": "0.0.1",
  "description": "Client component to handle scrolling of large-lists",
  "main": "dist/scrolly-bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "",
    "compile": "rm -rf dist/ && npm run compile-ts && cp lib/BinaryIndexedTree* dist/cjs/lib",
    "build": "webpack",
    "lint": "tslint --project .",
    "compile-ts": "tsc -p ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "grainjs": "^0.1.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1",
    "underscore": "^1.9.1",
    "webpack": "^4.17.0",
    "webpack-command": "^0.4.1"
  }
}
